You could also consider:

Implementing a cache for results to avoid repeated checks
Adding more varied user agents to avoid rate limiting
Using a headless browser like Playwright or Puppeteer for more reliable web scraping
Consider caching results to avoid unnecessary API calls
